name: pfirsichfest-bot
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.13
  - pip
  - pytest
  - pip:
      - fastapi
      - uvicorn
      - httpx
      - google-cloud-compute
      - google-cloud-secret-manager
      - google-cloud-firestore
      - aiogram
      - python-dotenv
